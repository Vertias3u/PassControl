openapi: 3.1.0
info:
  title: PassControl Control-Plane API
  version: "1.0.0"
  description: >
    Developer API for managing a PassControl agent fleet. Authenticated with a
    `pc_` API key (Authorization: Bearer). All access is tenant-isolated to the
    key's owner. Reads need a `read` key; writes need a `write` key. Mutations
    accept an `Idempotency-Key` header. See DOCUMENTATION.md for prose docs.
servers:
  - url: "{gateway}/api/control/v1"
    variables:
      gateway:
        default: https://your-gateway.example.com
        description: Your PassControl deployment origin.
security:
  - bearerAuth: []

paths:
  /agents:
    get:
      summary: List agents
      operationId: listAgents
      parameters:
        - { $ref: "#/components/parameters/Status" }
        - { $ref: "#/components/parameters/Limit" }
      responses:
        "200":
          description: The caller's agents.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { type: array, items: { $ref: "#/components/schemas/Agent" } }
        "401": { $ref: "#/components/responses/Error" }
        "429": { $ref: "#/components/responses/Error" }
    post:
      summary: Create an agent
      operationId: createAgent
      parameters:
        - { $ref: "#/components/parameters/IdempotencyKey" }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AgentCreate" }
      responses:
        "201":
          description: Created.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties: { id: { type: string, format: uuid }, name: { type: string } }
        "403": { $ref: "#/components/responses/Error" }
        "409": { $ref: "#/components/responses/Error" }
        "422": { $ref: "#/components/responses/Error" }

  /agents/{id}:
    parameters:
      - { $ref: "#/components/parameters/AgentId" }
    get:
      summary: Get an agent
      operationId: getAgent
      responses:
        "200":
          description: The agent.
          content:
            application/json:
              schema:
                type: object
                properties: { data: { $ref: "#/components/schemas/Agent" } }
        "404": { $ref: "#/components/responses/Error" }
    patch:
      summary: Update an agent (name / scopes / budgets)
      operationId: updateAgent
      parameters:
        - { $ref: "#/components/parameters/IdempotencyKey" }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AgentUpdate" }
      responses:
        "200": { $ref: "#/components/responses/AgentIdOk" }
        "400": { $ref: "#/components/responses/Error" }
        "404": { $ref: "#/components/responses/Error" }
        "422": { $ref: "#/components/responses/Error" }
    delete:
      summary: Revoke an agent (terminal)
      operationId: revokeAgent
      parameters:
        - { $ref: "#/components/parameters/IdempotencyKey" }
      responses:
        "200": { $ref: "#/components/responses/AgentStatusOk" }
        "404": { $ref: "#/components/responses/Error" }

  /agents/{id}/suspend:
    post:
      summary: Suspend an agent
      operationId: suspendAgent
      parameters:
        - { $ref: "#/components/parameters/AgentId" }
        - { $ref: "#/components/parameters/IdempotencyKey" }
      responses:
        "200": { $ref: "#/components/responses/AgentStatusOk" }
        "404": { $ref: "#/components/responses/Error" }

  /agents/{id}/trace:
    post:
      summary: Evaluate a read-only gateway decision snapshot
      operationId: traceAgentDecision
      description: >
        Evaluates kill, suspend, scope, endpoint, policy, and budget in proxy
        order without reserving budget, consuming the agent's hourly policy
        counter, resolving a provider key, or writing a gateway log. The result
        is a point-in-time snapshot and is never cached.
      parameters:
        - { $ref: "#/components/parameters/AgentId" }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [provider, model]
              properties:
                provider: { type: string, enum: [openai, anthropic, groq, mistral, together, deepseek, gemini] }
                model: { type: string, minLength: 1, maxLength: 200 }
                timestamp:
                  type: string
                  format: date-time
                  description: Optional UTC policy-window clock; defaults to evaluation time.
      responses:
        "200":
          description: Non-cacheable point-in-time decision snapshot.
          headers:
            Cache-Control:
              schema: { type: string, const: no-store }
          content:
            application/json:
              schema:
                type: object
                properties: { data: { $ref: "#/components/schemas/DecisionTrace" } }
        "400": { $ref: "#/components/responses/Error" }
        "404": { $ref: "#/components/responses/Error" }
        "429": { $ref: "#/components/responses/Error" }

  /agents/{id}/resume:
    post:
      summary: Resume a suspended agent
      operationId: resumeAgent
      parameters:
        - { $ref: "#/components/parameters/AgentId" }
        - { $ref: "#/components/parameters/IdempotencyKey" }
      responses:
        "200": { $ref: "#/components/responses/AgentStatusOk" }
        "404": { $ref: "#/components/responses/Error" }

  /logs:
    get:
      summary: List gateway call logs
      operationId: listLogs
      parameters:
        - { $ref: "#/components/parameters/AgentIdQuery" }
        - { $ref: "#/components/parameters/Status" }
        - { $ref: "#/components/parameters/CallClass" }
        - { $ref: "#/components/parameters/Limit" }
      responses:
        "200":
          description: Recent gateway calls.
          content:
            application/json:
              schema:
                type: object
                properties: { data: { type: array, items: { $ref: "#/components/schemas/Log" } } }

  /audit:
    get:
      summary: List admin-action audit events
      operationId: listAudit
      parameters:
        - { $ref: "#/components/parameters/Limit" }
      responses:
        "200":
          description: Admin-action trail.
          content:
            application/json:
              schema:
                type: object
                properties: { data: { type: array, items: { $ref: "#/components/schemas/AuditEvent" } } }

  /spend:
    get:
      summary: Per-agent and fleet spend
      operationId: getSpend
      responses:
        "200":
          description: Spend rollup (cost in micro-cents; USD = µ¢ / 100,000,000).
          content:
            application/json:
              schema:
                type: object
                properties: { data: { $ref: "#/components/schemas/Spend" } }

  /kill-switch:
    get:
      summary: Get the per-tenant kill-switch state
      operationId: getKillSwitch
      responses:
        "200":
          description: Current state.
          content:
            application/json:
              schema:
                type: object
                properties: { data: { $ref: "#/components/schemas/KillState" } }
    put:
      summary: Arm or disarm the per-tenant master kill switch
      operationId: setKillSwitch
      parameters:
        - { $ref: "#/components/parameters/IdempotencyKey" }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [armed]
              properties: { armed: { type: boolean } }
      responses:
        "200":
          description: New state.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties: { armed: { type: boolean }, affected: { type: integer } }
        "422": { $ref: "#/components/responses/Error" }

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      description: A `pc_` developer API key.

  parameters:
    AgentId:
      name: id
      in: path
      required: true
      schema: { type: string, format: uuid }
    AgentIdQuery:
      name: agent_id
      in: query
      schema: { type: string, format: uuid }
    Status:
      name: status
      in: query
      schema: { type: string }
    CallClass:
      name: class
      in: query
      description: >-
        Narrow to operationally meaningful calls (`inference`) or to SDK
        housekeeping such as startup model-listing probes (`housekeeping`).
        Omitted returns every recorded call, which stays the default: the log is
        the complete record, and this parameter only chooses a view of it.
        Classification is derived from the stored row, not a stored field, and a
        refused call is never housekeeping.
      schema: { type: string, enum: [inference, housekeeping] }
    Limit:
      name: limit
      in: query
      description: >-
        Applied to the query before any `class` filter, so a filtered page can
        return fewer rows than the limit.
      schema: { type: integer, minimum: 1, maximum: 100, default: 50 }
    IdempotencyKey:
      name: Idempotency-Key
      in: header
      required: false
      schema: { type: string, maxLength: 200 }
      description: Retrying with the same key will not re-apply the mutation.

  responses:
    Error:
      description: Error.
      content:
        application/json:
          schema: { $ref: "#/components/schemas/Error" }
    AgentIdOk:
      description: OK.
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                type: object
                properties: { id: { type: string, format: uuid } }
    AgentStatusOk:
      description: OK.
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                type: object
                properties: { id: { type: string, format: uuid }, status: { type: string } }

  schemas:
    Scope:
      type: object
      properties:
        provider: { type: string, enum: [openai, anthropic, groq, mistral, together, deepseek, gemini] }
        models: { type: array, items: { type: string } }
    Agent:
      type: object
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        passport_pubkey: { type: string, description: base64url Ed25519 public key }
        status: { type: string, enum: [active, suspended, revoked] }
        budget_tokens: { type: [integer, "null"] }
        budget_cents: { type: [integer, "null"] }
        spent_tokens: { type: integer }
        spent_microcents: { type: integer }
        allowed_scopes: { type: array, items: { $ref: "#/components/schemas/Scope" } }
        created_at: { type: string, format: date-time }
        last_seen_at: { type: [string, "null"], format: date-time }
    AgentCreate:
      type: object
      required: [name, passportPubkey, scopes]
      properties:
        name: { type: string, minLength: 1, maxLength: 80 }
        passportPubkey: { type: string, description: base64url Ed25519 public key (you keep the private key) }
        scopes: { type: array, items: { $ref: "#/components/schemas/Scope" } }
        budget_tokens: { type: [integer, "null"], minimum: 0 }
        budget_cents: { type: [integer, "null"], minimum: 0 }
    AgentUpdate:
      type: object
      description: Partial — send only the fields to change.
      properties:
        name: { type: string, minLength: 1, maxLength: 80 }
        scopes: { type: array, items: { $ref: "#/components/schemas/Scope" } }
        budget_tokens: { type: [integer, "null"], minimum: 0 }
        budget_cents: { type: [integer, "null"], minimum: 0 }
    GateStep:
      type: object
      required: [name, status, reason]
      properties:
        name: { type: string, enum: [kill, suspend, scope, endpoint, policy, budget] }
        status: { type: string, enum: [pass, fail, skipped] }
        reason: { type: string }
        rule: { type: string }
        httpStatus: { type: integer, enum: [402, 403, 429] }
        presentation: { type: string, enum: [normal, warning] }
    DecisionTrace:
      type: object
      required: [snapshot, evaluated_at, policy_time, agent_id, provider, model, method, path, verdict, steps]
      properties:
        snapshot: { type: boolean, const: true }
        evaluated_at: { type: string, format: date-time }
        policy_time: { type: string, format: date-time }
        agent_id: { type: string, format: uuid }
        provider: { type: string, enum: [openai, anthropic, groq, mistral, together, deepseek, gemini] }
        model: { type: string }
        method: { type: string, const: POST }
        path: { type: array, items: { type: string } }
        verdict: { type: string, enum: [allow, deny] }
        denied_by: { type: string, enum: [kill, suspend, scope, endpoint, policy, budget] }
        policy:
          type: object
          properties:
            outcome: { type: string, enum: [allow, deny_by_rule, unreadable] }
            posture: { type: string, enum: [fail_open, fail_closed] }
        steps: { type: array, items: { $ref: "#/components/schemas/GateStep" } }
    Log:
      type: object
      properties:
        id: { type: string, format: uuid }
        agent_id: { type: [string, "null"], format: uuid }
        passport_id: { type: string }
        jti: { type: string }
        provider: { type: [string, "null"] }
        model: { type: [string, "null"] }
        input_tokens: { type: [integer, "null"] }
        output_tokens: { type: [integer, "null"] }
        cost_microcents: { type: [integer, "null"] }
        status: { type: string }
        latency_ms: { type: [integer, "null"] }
        created_at: { type: string, format: date-time }
    AuditEvent:
      type: object
      properties:
        id: { type: string, format: uuid }
        action: { type: string }
        target_type: { type: [string, "null"] }
        target_id: { type: [string, "null"] }
        metadata: { type: object, additionalProperties: true }
        created_at: { type: string, format: date-time }
    Spend:
      type: object
      properties:
        fleet:
          type: object
          properties: { spent_tokens: { type: integer }, spent_microcents: { type: integer } }
        agents:
          type: array
          items:
            type: object
            properties:
              id: { type: string, format: uuid }
              name: { type: string }
              spent_tokens: { type: integer }
              spent_microcents: { type: integer }
    KillState:
      type: object
      properties:
        armed: { type: boolean }
        platform_kill: { type: boolean }
    Error:
      type: object
      properties:
        error:
          type: object
          properties:
            code: { type: string }
            message: { type: string }
            request_id: { type: string }
